---
interface Props {
  title?: string;
  body?: string;
}

const { title, body } = Astro.props;
---

<>
  {title && <h2 class="text-20px font-bold m-0 z-20 ">{title}</h2>}
  {body && <p class="m-0 font-light text-base">{body}</p>}
  <slot />
</>
